rootProject.name = 'Energy-Is-Matter' // Deprecation warnings otherwise

includeBuild('Boson') {
    dependencySubstitution {
        substitute module('net.thesilkminer.mc:boson') with project(':')
    }
}
